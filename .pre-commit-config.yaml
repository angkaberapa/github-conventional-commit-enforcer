repos:
  - repo: local
    hooks:
      - id: validate-commit-msg
        name: Validate conventional commit message
        entry: validate-commit-msg
        language: python
        stages: [commit-msg]
        additional_dependencies: [PyYAML]
